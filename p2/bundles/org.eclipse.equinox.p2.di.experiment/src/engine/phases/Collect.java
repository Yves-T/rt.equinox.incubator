package engine.phases;

import repository.RepositoryManager;
import engine.ActionManager;

public class Collect {
	public Collect(ActionManager mgr, RepositoryManager repoMgr) { //It could also take just a list of repos or a ProvisioningContext
	}
}
